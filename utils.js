// Utility Functions

// Format Time
function formatTime() {
  return new Date().toLocaleString();
}

// Show Alert
function showMessage(msg) {
  alert(msg);
}

// Redirect
function goHome() {
  window.location.href = "../index.html";
}